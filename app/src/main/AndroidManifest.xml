<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.logicmind">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:hardwareAccelerated="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.LogicMind"
        tools:targetApi="31">

        <activity
            android:name=".activities.SettingsActivity"
            android:configChanges="locale|uiMode|screenSize|orientation|keyboardHidden"
            android:exported="false" />

        <activity
            android:name=".activities.ProfileActivity"
            android:exported="false" />

        <activity
            android:name=".activities.GameSelectionActivity"
            android:exported="false"
            android:configChanges="locale|layoutDirection"
            />

        <activity
            android:name=".activities.GameIntroActivity"
            android:exported="false"
            android:configChanges="locale|layoutDirection"
            />

        <activity
            android:name=".activities.CardMatchActivity"
            android:exported="false" />

        <activity
            android:name=".activities.NumberAdditionActivity"
            android:configChanges="locale|uiMode|screenSize|orientation|keyboardHidden"
            android:exported="false"/>

        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".activities.StatisticsActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".activities.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

    </application>

</manifest>
