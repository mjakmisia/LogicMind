<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <!-- Plik do zmiany kolorów jak język jest wybrany -->
    <solid android:color="?attr/pageBackground" />
    <corners android:radius="12dp"/>
    <stroke
        android:width="2dp"
        android:color="?attr/pageBackground" />
</shape>
