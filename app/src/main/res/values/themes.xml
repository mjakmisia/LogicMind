<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme for light mode -->
    <style name="Theme.LogicMind" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Background and surface -->
        <item name="android:windowBackground">@color/white</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnBackground">@color/textGrey</item>
        <item name="colorOnSurface">@color/textGrey</item>

        <!-- Error color -->
        <item name="colorError">@color/category_pink</item>
        <item name="colorOnError">@color/white</item>

        <!-- Switch colors -->
        <item name="switchTrackColor">@color/light_switch_track</item>
        <item name="switchThumbColor">@color/light_switch_thumb</item>
    </style>

</resources>
